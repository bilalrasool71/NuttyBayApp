export interface PurchaseOrderSplit {
    purchaseOrderSplitId?: number; // Represents the key property
    splitCode?: string; // Nullable string
    purchaseOrderId?: number; // Nullable integer
    qty?: number; // Nullable integer
  }
  